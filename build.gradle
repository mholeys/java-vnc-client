// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'eclipse'

// Disable the jar for this empty parent project
jar.enabled = false

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

description = ""
version = "pre-release"
